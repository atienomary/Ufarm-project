doctype html
head
  title Delivery Info
  link(rel='stylesheet' href='style.css')
style.
  form {
  display: flex;
  flex-direction: column;
  max-width: 500px;
  margin: 0 auto;
  }
  body {
  background-image: url("ufarm\ images/delivery1.jpg");
  background-repeat: repeat;
  width: 100%;
  height: 100vh;
  background-position: center;
  background-size: cover;
  }
  h1{
  text-align: center;
  justify-content: center;
  color: #4CAF50;
  }
  label {
  margin-top: 10px;
  font-weight: bold;
  color: #4CAF50;
  }
  input[type="text"],
  input[type="email"],
  textarea,
  select,
  input[type="number"] {
  padding: 5px;
  border: 1px solid #ccc;
  border-radius: 4px;
  margin-top: 5px;
  margin-bottom: 10px;
  font-size: 16px;
  }
  button[type="submit"] {
  background-color: #4CAF50;
  color: #fff;
  padding: 10px 20px;
  border: none;
  border-radius: 4px;
  cursor: pointer;
  font-size: 16px;
  }
  button[type="submit"]:hover {
  background-color: #3e8e41;
  }
  input:invalid,
  textarea:invalid,
  select:invalid {
  border-color: red;
  }
  .success {
  color: green;
  font-size: 18px;
  text-align: center;
  margin-top: 20px;
  }
h1 Product Delivery
form
  label(for='name') Name:
  input#name(type='text' name='name' required='')
  label(for='email') Email:
  input#email(type='email' name='email' required='')
  label(for='address') Delivery Address:
  textarea#address(name='address' rows='5' required='')
  label(for='product') Product:
  select#product(name='product' required='')
    option(value='') --Please select a product--
    option(value='product1') Fruits
    option(value='product2') Greens
    option(value='product3') Grains
  label(for='quantity') Quantity:
  input#quantity(type='number' name='quantity' min='1' max='10' required='')
  button(type='submit') Submit